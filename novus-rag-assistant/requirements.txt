openai>=1.0.0
langfuse>=2.0.0,<3.0.0
psycopg2-binary
pgvector
python-dotenv
rich
# Week 2 additions
rank-bm25          # production BM25 (optional — bm25_scratch.py works standalone)
litellm            # A3.2 stretch: model-agnostic LLM with fallback
ragas              # A3.1 core: RAGAS evaluation framework
datasets           # required by ragas
# Week 3 additions
numpy              # P1A.1: cosine similarity in SemanticCache
fastapi            # D1.1: API wrapper
uvicorn[standard]  # D1.1: ASGI server
requests           # D4.1: remote eval harness HTTP calls
gptcache           # P1A.3: GPTCache backend (optional — falls back to in-memory)
# Week 4 additions
instructor         # O2.2/S4.1: structured output via Pydantic + instructor (v1 API)
presidio-analyzer  # P3.1–P3.3: PII detection engine
presidio-anonymizer  # P3.1–P3.3: companion to presidio-analyzer
spacy              # required by presidio for NER (PERSON, LOCATION)
cohere>=4.0,<5.0   # Cohere reranker: rerank-english-v3.0 (opt-in via use_reranker=True)
